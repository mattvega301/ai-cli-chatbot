# AI CLI Chatbot

A command-line chatbot built with Python and the OpenAI API.
Supports multi-turn conversations with full memory across the session.

## Setup
1. Clone the repo
2. Install dependencies: pip install openai python-dotenv
3. Create a .env file with your OPENAI_API_KEY
4. Run: python3 chatbot.py